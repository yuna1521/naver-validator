# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

브랜드사가 제출한 "애드웰 상품제안서"(`.xlsx`)를 업로드 받아, 각 상품의 기재 최저가가 실제 네이버 쇼핑 최저가와 정합한지 자동으로 검수하는 Streamlit 웹 앱이다. 결과는 원본 엑셀의 우측(Q열 이후)에 보존되어 다운로드된다.

## 주요 명령

```powershell
# 의존성 설치 (venv 사용 권장; 이미 ./venv/ 존재)
pip install -r requirements.txt

# 로컬 실행
streamlit run app.py
```

테스트 프레임워크나 lint 설정은 현재 없다. 검수 동작은 `app.py`의 "테스트 모드" 체크박스(처음 N건만 처리)로 수동 확인한다.

## API 자격 정보

네이버 쇼핑 API 키가 필요하다. 두 가지 경로 중 하나로 주입된다 (`core.py:13-23`):

1. 로컬: `.env`의 `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET` (python-dotenv가 자동 로드)
2. Streamlit Cloud: `.streamlit/secrets.toml` → `app.py`에서 `set_credentials()`로 주입

두 파일 모두 `.gitignore`에 포함되어 있다. 키 누락 시 검수 결과는 `"API 키 누락"` 메시지를 남긴다.

## 아키텍처

3개 모듈로 명확히 분리되어 있다:

- **`app.py`** — Streamlit UI 전담. 파일 업로드/미리보기, 진행률 표시, 필터링된 결과 테이블, 다운로드 버튼. `st.session_state.results`에 검수 결과를 보관하여 사이드바 필터 조작으로 페이지가 재실행되어도 결과가 유지되게 한다.
- **`core.py`** — 검수 로직 전체. `validate_row()`가 진입점이며, 한 행에 대해 네이버 검색 → 매칭/필터링 → 가격 비교 → 메모 생성 → "승인 가능"/"확인 필요" 판정을 수행한다.
- **`excel_writer.py`** — `openpyxl`로 원본 워크북을 그대로 로드한 뒤 Q열부터 검수 결과 5개 컬럼을 덧붙인다. 원본 시트의 서식·수식·셀 병합을 모두 보존하는 것이 목적.

`archive/`는 초기 개발 단계의 단계별 스크립트(step1~step4)로, 현재 실행 경로에서는 사용하지 않는다.

### 양식 의존성 (중요)

이 앱은 "애드웰 상품제안서" 양식 **전용**이다. 양식 가정이 코드 곳곳에 박혀 있다:

- 엑셀 첫 시트, 1행은 제목, **2행이 헤더, 3행부터 데이터** (`pd.read_excel(..., header=1)`, `excel_writer.py:18 DATA_START_ROW = 3`)
- 컬럼 매핑은 `core.py:29 COL_MAP`에 정의. 키에 **줄바꿈이 포함된 문자열**이 있다 (예: `"정가\n(A)"`, `"공급가\n(B)"`). 양식 변경 시 이 dict가 깨질 수 있으므로 함께 수정한다.
- 원본의 컬럼 수는 **16개(A~P)**라고 가정 (`excel_writer.py:15 ORIG_COL_COUNT = 16`). 양식이 늘어나면 검수 컬럼 시작 위치도 옮겨야 한다.
- 판매가(D)와 할인율은 엑셀 수식이지만 `pd.read_excel`은 수식 결과를 못 읽는 경우가 있어 `app.py`와 `core.py`가 **수식을 직접 재계산**한다 (`판매가 = 공급가 / (1 - 수수료율)`, 1의 자리에서 반올림).

### 네이버 검색 / 매칭 파이프라인

`search_naver_price()`는 단순한 API wrapper가 아니라 여러 단계의 필터/매칭을 수행한다. 디버깅 시 흐름:

1. **1차 검색**: 원본 상품명 그대로 네이버 쇼핑 API에 질의.
2. **1차 필터** (`_do_naver_search` 내부): `productType` 제외, 브랜드 일치 확인, 중고/리퍼/B급/반품/전시품/미사용/개봉 키워드 제외, 기재 최저가의 30%~300% 범위 밖 가격 제외.
3. **2차 매칭** (`match_score`):
   - 원본 상품명에서 모델 코드(예: `AQ-75`, `ICE-43T`) 추출 후 결과 제목과 비교.
   - **다른 모델 코드가 결과 제목에 있으면 즉시 0점** — 같은 브랜드의 다른 모델이 매칭되는 문제 방지 (예: `AQ-101` 검색에 `AQ-120`이 잡히는 케이스).
   - 모델 코드가 없으면 단위(`75ml`, `2kg` 등)와 키워드 매칭으로 fallback. fallback 점수는 `MODEL_FALLBACK_SCORE = 0.7`.
   - 임계값 `MATCH_THRESHOLD = 0.7` 이상이어야 후보 채택.
4. **완화 재검색**: 1차가 `NO_MATCH`면 `_strip_parens()`로 괄호 안 옵션(색상/사이즈)을 제거하고 한 번 더 검색. 성공 시 메모에 `"완화 검색으로 매칭"` 명시.

### API rate limiting

- 매 호출 전 100ms 딜레이 (초당 호출 제한 회피).
- `429` 응답 시 2초 대기 후 1회 재시도. 재시도도 `429`면 `API_LIMIT` 반환 → `validate_row`가 `{"_api_limit": True}`만 담은 dict를 반환 → 상위 루프가 즉시 중단되고 그 시점까지의 결과만 표시.

### 판정 로직

`"승인 가능"`은 **모든** 조건을 만족할 때만 부여된다 (`core.py:445-458`):
- 매칭 OK
- 우리 판매가 ≤ 네이버 조회가 (가격 경쟁력)
- 기재 최저가와 조회가의 차이가 `tolerance_pct` (기본 5%, 사이드바에서 1~20% 조정) 이내
- 가격 변동성 이상치 없음 (`detect_price_anomaly`: 1위가 2위의 1.5배보다 저렴하면 일회성 특가 가능성)

위 중 하나라도 실패하면 `"확인 필요"`, 각 실패 사유는 `검수 메모`에 ` / `로 연결된다.

## 코드 변경 시 주의

- `COL_MAP` 키와 헤더 셀 문자열은 **완전히 일치**해야 한다. 줄바꿈/공백 한 글자만 달라져도 `KeyError`가 아니라 조용히 `None`이 반환되어 검수가 잘못된다.
- `excel_writer.py`의 `ORIG_COL_COUNT`와 `DATA_START_ROW`는 양식 가정이다. 양식이 바뀌면 함께 갱신한다.
- 한국어 식별자(`판매가`, `기재최저가`, `메모조각` 등)와 영문 식별자가 혼재한다. 기존 스타일을 유지한다.
- 결과 dict에는 내부 신호용 `_api_limit` 키가 잠시 포함되었다가 호출자가 `pop`으로 제거한다. 새로 추가하는 필드도 결과 dict 형태(`결과`/`네이버 조회가`/`차이`/`시장 풍부도`/`검수 메모`)와 일관되게 유지한다 — 이 키들은 `app.py` 표시, `excel_writer.py` 출력에서 그대로 참조된다.
